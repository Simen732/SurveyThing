<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Survey Application</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="/css/styles.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="fas fa-poll me-2"></i>Survey App
            </a>
            
            <div class="navbar-nav ms-auto">
                <% if (typeof user !== 'undefined' && user) { %>
                    <span class="navbar-text me-3">
                        Welcome, <%= user.username %>
                    </span>
                    <a href="/admin/dashboard" class="btn btn-outline-light btn-sm me-2">Dashboard</a>
                    <form action="/auth/logout" method="POST" class="d-inline">
                        <button type="submit" class="btn btn-outline-light btn-sm">
                            <i class="fas fa-sign-out-alt"></i> Logout
                        </button>
                    </form>
                <% } else { %>
                    <a href="/auth/login" class="btn btn-outline-light btn-sm">
                        <i class="fas fa-sign-in-alt"></i> Admin Login
                    </a>
                <% } %>
            </div>
        </div>
    </nav>

    <main class="container mt-4">
        <div class="row">
            <div class="col-lg-12">
                <div class="hero-section text-center mb-5">
                    <h1 class="display-4 text-primary mb-3">
                        <i class="fas fa-clipboard-list me-3"></i>
                        Professional Survey Platform
                    </h1>
                    <p class="lead text-muted mb-4">
                        Share your feedback and help us improve our workplace experience
                    </p>
                </div>

                <div class="row">
                    <div class="col-lg-8 mx-auto">
                        <div class="card shadow-sm">
                            <div class="card-body p-5">
                                <h2 class="card-title text-center mb-4">Available Surveys</h2>
                                <div class="text-center">
                                    <a href="/survey" class="btn btn-primary btn-lg">
                                        <i class="fas fa-play me-2"></i>
                                        View Available Surveys
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="row mt-5">
                    <div class="col-md-4">
                        <div class="feature-card text-center p-4">
                            <div class="feature-icon mb-3">
                                <i class="fas fa-user-secret fa-3x text-primary"></i>
                            </div>
                            <h5>Anonymous</h5>
                            <p class="text-muted">Your responses are completely anonymous and secure.</p>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="feature-card text-center p-4">
                            <div class="feature-icon mb-3">
                                <i class="fas fa-clock fa-3x text-primary"></i>
                            </div>
                            <h5>Quick & Easy</h5>
                            <p class="text-muted">Complete surveys in just a few minutes.</p>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="feature-card text-center p-4">
                            <div class="feature-icon mb-3">
                                <i class="fas fa-chart-bar fa-3x text-primary"></i>
                            </div>
                            <h5>Meaningful Impact</h5>
                            <p class="text-muted">Your feedback drives positive workplace changes.</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <footer class="bg-light mt-5 py-4">
        <div class="container text-center text-muted">
            <p>&copy; 2024 Survey Application. Professional feedback collection system.</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>